% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ETL.R
\name{run_etl_pipeline}
\alias{run_etl_pipeline}
\title{Run ETL pipeline}
\usage{
run_etl_pipeline(
  mode,
  repo_url = NULL,
  local_path = NULL,
  clone_dir = NULL,
  github_token = NULL
)
}
\arguments{
\item{mode}{0 = local, 1 = remote}

\item{repo_url}{GitHub URL (for mode = 1)}

\item{local_path}{Path to local repo (for mode = 0)}

\item{clone_dir}{Directory for cloning (for mode = 1, optional)}

\item{github_token}{GitHub personal access token (optional, for higher rate limits)}
}
\value{
List with status, message, repo_path, and db_path
}
\description{
Main entry point for the ETL (Extract, Transform, Load) process.
Clones or updates a Git repository, extracts commit history and code
changes, and loads them into a DuckDB database.
}
\examples{
\dontrun{
# Local repository
result <- run_etl_pipeline(mode = 0, local_path = "D:/my_repos/project")

# Remote repository
result <- run_etl_pipeline(
  mode = 1,
  repo_url = "https://github.com/aaalyaaa/dbipAnalyzer.git",
  clone_dir = "D:/repos"
)
print(result$status)
}

}
